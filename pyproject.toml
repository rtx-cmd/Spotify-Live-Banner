[project]
name = "spotify-live-banner"
version = "1.0.0"
description = "Spotify Live Banner"
requires-python = ">=3.9"
dependencies = [
    "Flask>=3.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
    "Werkzeug>=3.0.0",
    "Pillow>=10.0.0",
    "gunicorn>=21.2.0"
]

[tool.vercel]
entrypoint = "source.main:app"
